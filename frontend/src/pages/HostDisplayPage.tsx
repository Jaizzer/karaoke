// TV/tablet-oriented, unlike every other page: what's on screen connected to the sound system.
import HostDashboard from '../features/room-host/HostDashboard.tsx';

export default function HostDisplayPage() {
	return <HostDashboard />;
}
